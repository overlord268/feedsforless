# API base URL (used at build time in production).
# Local dev: leave unset or use http://localhost:8000
# Production (Railway): set to your API URL, e.g.:
# VITE_API_URL=https://feedsforless-api-production.up.railway.app
VITE_API_URL=
